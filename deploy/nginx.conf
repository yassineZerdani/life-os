# Replace with your domain if different. Root = where frontend static files are.

server {
    listen 80;
    server_name aeropyxtech.com www.aeropyxtech.com;

    root /var/www/lifeos/frontend-dist;
    index index.html;

    # Frontend SPA: serve static and fallback to index.html for client-side routing
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    # API proxy to backend (no trailing slash in proxy_pass to preserve path)
    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    location /health {
        proxy_pass http://127.0.0.1:8000/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
}

# Optional: HTTPS with Let's Encrypt (certbot)
# After getting cert: certbot --nginx -d yourdomain.com
# server {
#     listen 443 ssl;
#     server_name yourdomain.com;
#     ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
#     ... same location blocks as above ...
# }
